Changing the local database to use the PROD sql dump steps

1. Bring up the command prompt.

2. And type mysql -u root -proot to connect to localhost mysql server.

3. Drop schema mat_app;

4. Create Schema mat_app;

5. use mat_app;

6. source R:\PUBLIC\Development\Deployments\NQF\Nqf_Prod_0616_123pm.sql

7. Select * from user where first_name like 'Chris';

8. Then follow the script under Mat/sql/RESET_USER_EMAIL.sql
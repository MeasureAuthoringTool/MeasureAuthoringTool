<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog 
		http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-2.0.xsd" >

    <changeSet
        id="1"
        author="mat_dev_user"
        context="prod" >

        <sql>
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'result','19','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'result','36','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'result','57','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'result','24','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'result','31','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'status','19','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'status','31','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'status','36','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'patient preference','104','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'provider preference','104','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'cumulative medication duration','39','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'method','61','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'ordinality','61','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'radiation duration','62','Data Type');
		</sql>
    </changeSet>
</databaseChangeLog>
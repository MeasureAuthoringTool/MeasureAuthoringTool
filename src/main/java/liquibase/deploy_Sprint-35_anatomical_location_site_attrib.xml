<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog 
		http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-2.0.xsd" >

    <changeSet
        id="1"
        author="mat_dev_user"
        context="prod" >

        <sql>
            insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','7','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','10','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','9','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','13','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','81','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','30','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','31','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','56','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','57','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','87','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','62','Data Type');
			
			insert into QDM_ATTRIBUTES values 
			((select concat(max(cast(aw.ID as signed))+1,'') from QDM_ATTRIBUTES aw),
			'anatomical location site','63','Data Type');
        </sql>
    </changeSet>
</databaseChangeLog>